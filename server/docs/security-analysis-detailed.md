# ДЕТАЛЬНИЙ АНАЛІЗ SECURITY МЕТОДІВ

## 📋 Структура analyse_security.json

```json
{
  "honeypot_check": {
    "checked_by": ["dexscreener_txns", "rpc_recent_txs"],
    "buy_possible": null,
    "sell_possible": true,
    "honeypot": false,
    "reasons": [
      "DexScreener reports sells=6756 in recent windows",
      "RPC parsed recent txs sells_found=0"
    ]
  },
  "lp_owner": "pAMMBay6oceH9fJKBRHGP5D4bD4sWpmSwMn52FMfXEA",
  "dev_address": "BrhPVH7T39j3wBdMAwiUqHY3w23ZP6UCxvJDmK46fv71"
}
```

---

## 🔍 1. HONEYPOT CHECK (`_honeypot_with_fallback`)

**Локація:** `_v1_analyzer_async.py`, рядки 1199-1281

### Алгоритм (3 рівні перевірки):

```
┌─────────────────────────────────────────────────────────────┐
│                    HONEYPOT DETECTION                        │
└─────────────────────────────────────────────────────────────┘
                           │
                           ▼
        ┌──────────────────────────────────────┐
        │  1️⃣ МЕТОД 1: Jupiter Quote API      │
        │  (Основний метод)                     │
        └──────────────────────────────────────┘
                           │
          ┌────────────────┴────────────────┐
          │                                  │
          ▼ Успіх                           ▼ Помилка
    ┌─────────────┐                   ┌─────────────┐
    │   RETURN    │                   │  Fallback   │
    │   Result    │                   │  Method 2   │
    └─────────────┘                   └─────────────┘
                                              │
                                              ▼
                        ┌──────────────────────────────────────┐
                        │  2️⃣ МЕТОД 2: DexScreener txns       │
                        │  (Підрахунок sells)                  │
                        └──────────────────────────────────────┘
                                              │
                                              ▼
                        ┌──────────────────────────────────────┐
                        │  3️⃣ МЕТОД 3: Solana RPC txs         │
                        │  (Аналіз балансів)                   │
                        └──────────────────────────────────────┘
                                              │
                                              ▼
                                        ┌─────────────┐
                                        │   RETURN    │
                                        │   Result    │
                                        └─────────────┘
```

### 1️⃣ Метод 1: Jupiter Quote API (рядки 1204-1223)

**Що робить:**
```python
# Перевірка купівлі
buy_url = "https://quote-api.jup.ag/v6/quote?inputMint=SOL&outputMint={token}&amount=1000000"

# Перевірка продажу
sell_url = "https://quote-api.jup.ag/v6/quote?inputMint={token}&outputMint=SOL&amount=1000000"
```

**Логіка:**
```python
buy_res = await fetch(buy_url)   # Спроба купити токен за SOL
sell_res = await fetch(sell_url)  # Спроба продати токен за SOL

if buy_ok and sell_ok:
    return {
        "checked_by": ["jupiter_quote_api"],
        "buy_possible": True,
        "sell_possible": True,
        "honeypot": False  # ✅ Можна купувати і продавати
    }
elif buy_ok and not sell_ok:
    return {
        "honeypot": True  # ⚠️ HONEYPOT! Купити можна, продати - ні
    }
```

**Приклад результату:**
```json
{
  "checked_by": ["jupiter_quote_api"],
  "buy_possible": true,
  "sell_possible": false,
  "honeypot": true,
  "reasons": ["Jupiter quote sell failed or returned error"]
}
```

---

### 2️⃣ Метод 2: DexScreener Transactions (рядки 1225-1244)

**Що робить:**
```python
# Підраховує кількість продажів за всі часові вікна
sells = 0
for window in ("m5", "h1", "h6", "h24"):
    sells += txns[window]["sells"]

# m5  = 5 хвилин
# h1  = 1 година
# h6  = 6 годин
# h24 = 24 години
```

**Логіка:**
```python
if sells > 0:
    # Якщо є продажі - NOT honeypot
    result["sell_possible"] = True
    result["honeypot"] = False
    result["reasons"].append(f"DexScreener reports sells={sells} in recent windows")
else:
    # Якщо немає продажів - HONEYPOT
    result["sell_possible"] = False
    result["honeypot"] = True
    result["reasons"].append("DexScreener reports zero sells in recent windows")
```

**Приклад з вашого JSON:**
```json
{
  "txns": {
    "m5":  {"buys": 889,  "sells": 741},   // 741 продаж
    "h1":  {"buys": 2678, "sells": 2005},  // 2005 продажів
    "h6":  {"buys": 2678, "sells": 2005},  // 2005 продажів
    "h24": {"buys": 2678, "sells": 2005}   // 2005 продажів
  }
}

// Загальна сума продажів:
sells = 741 + 2005 + 2005 + 2005 = 6756

// Результат:
{
  "checked_by": ["dexscreener_txns"],
  "sell_possible": true,  // ✅ 6756 > 0
  "honeypot": false,      // ✅ NOT honeypot
  "reasons": ["DexScreener reports sells=6756 in recent windows"]
}
```

---

### 3️⃣ Метод 3: Solana RPC Transactions (рядки 1246-1272)

**Що робить:**
```python
# Аналізує останні транзакції токена
# Порівнює баланси ДО і ПІСЛЯ транзакції
parsed_txs = solana_rpc_data["recent_transactions_parsed"]

for tx in parsed_txs:
    pre_balance  = tx.meta.preTokenBalances[i].uiAmount   # До транзакції
    post_balance = tx.meta.postTokenBalances[i].uiAmount  # Після транзакції
    
    if post_balance < pre_balance:
        sells_found += 1  # ✅ Знайшли продаж (баланс зменшився)
```

**Візуалізація транзакції:**
```
Транзакція #1:
┌─────────────────┐        ┌─────────────────┐
│  PRE BALANCE    │        │  POST BALANCE   │
│                 │   →    │                 │
│  1000 tokens    │        │  900 tokens     │
└─────────────────┘        └─────────────────┘
      
1000 > 900  ✅ SELL DETECTED (продаж 100 токенів)
```

**Логіка:**
```python
if sells_found > 0:
    result["sell_possible"] = True
    result["honeypot"] = False
else:
    result["sell_possible"] = False
    result["honeypot"] = True

result["reasons"].append(f"RPC parsed recent txs sells_found={sells_found}")
```

**Приклад результату:**
```json
{
  "checked_by": ["rpc_recent_txs"],
  "sell_possible": false,  // ❌ Немає продажів в RPC
  "honeypot": true,        // ⚠️ Можливо honeypot
  "reasons": ["RPC parsed recent txs sells_found=0"]
}
```

---

### 🔀 Комбінований результат (Fallback)

Якщо метод 1 (Jupiter) не працює, використовуються методи 2 і 3:

```json
{
  "checked_by": [
    "dexscreener_txns",  // ✅ Знайшов 6756 продажів
    "rpc_recent_txs"     // ❌ Не знайшов продажів
  ],
  "buy_possible": null,
  "sell_possible": true,      // ✅ TRUE (бо DexScreener знайшов продажі)
  "honeypot": false,          // ✅ NOT honeypot
  "reasons": [
    "DexScreener reports sells=6756 in recent windows",  // ✅
    "RPC parsed recent txs sells_found=0"                // ⚠️ Суперечність
  ]
}
```

**Пріоритет методів:**
```
DexScreener > Solana RPC

Якщо DexScreener каже "sell_possible = true",
то результат буде "honeypot = false",
навіть якщо RPC не знайшов продажів.
```

---

## 👨‍💻 2. DEV ADDRESS (`_extract_dev_from_jupiter`)

**Локація:** `_v1_analyzer_async.py`, рядки 1283-1296

### Алгоритм:

```python
def _extract_dev_from_jupiter(jupiter_data):
    # Перевіряємо чи це список (багато токенів)
    if isinstance(jupiter_data, list) and jupiter_data:
        first = jupiter_data[0]
        # Шукаємо в різних форматах
        return first.get("dev") or \
               first.get("dev_address") or \
               first.get("devAddress")
    
    # Перевіряємо чи це словник (один токен)
    if isinstance(jupiter_data, dict):
        for key in ("dev", "dev_address", "devAddress"):
            if key in jupiter_data:
                return jupiter_data[key]
    
    return None
```

### Приклад Jupiter відповіді:

```json
{
  "id": "EK7Ms6Q9u3KZWBp5UeBUiC8Zb7CbGnFgxYmkTSvFSGyG",
  "name": "khole trade",
  "symbol": "KHOLE",
  "dev": "BrhPVH7T39j3wBdMAwiUqHY3w23ZP6UCxvJDmK46fv71",  // ✅ Знайдено тут
  "circSupply": 999998082.690627,
  "totalSupply": 999998082.690627
}
```

### Результат:

```json
{
  "dev_address": "BrhPVH7T39j3wBdMAwiUqHY3w23ZP6UCxvJDmK46fv71"
}
```

**Звідки береться:**
- 📊 **Джерело:** Jupiter API (`/tokens/v2/search`)
- 🔑 **Поле:** `dev`, `dev_address`, або `devAddress`
- 📝 **Що це:** Solana адреса розробника токена

---

## 💧 3. LP OWNER (`_get_lp_owner`)

**Локація:** `_v1_analyzer_async.py`, рядки 1185-1197

### Алгоритм:

```python
async def _get_lp_owner(pair_address):
    # 1. Отримуємо pair_address з DexScreener
    pair = dexscreener_data["pairs"][0]["pairAddress"]
    # Наприклад: "4FRUEUD7Z263sy3gtmrRUqMFPcBui2NsC9iJ4c63kMs7"
    
    # 2. Запитуємо Solana RPC про цей аккаунт
    payload = {
        "jsonrpc": "2.0",
        "id": 1,
        "method": "getAccountInfo",
        "params": [pair_address, {"encoding": "jsonParsed"}]
    }
    
    response = await solana_rpc.post(payload)
    
    # 3. Витягуємо owner з відповіді
    owner = response["result"]["value"]["owner"]
    
    return owner
```

### Візуалізація потоку:

```
┌──────────────────────────────────────────────────────────────┐
│  Крок 1: Отримати pair_address з DexScreener                 │
└──────────────────────────────────────────────────────────────┘
                           │
                           ▼
      DexScreener: pairs[0].pairAddress
      └─> "4FRUEUD7Z263sy3gtmrRUqMFPcBui2NsC9iJ4c63kMs7"
                           │
                           ▼
┌──────────────────────────────────────────────────────────────┐
│  Крок 2: Запит до Solana RPC getAccountInfo                  │
└──────────────────────────────────────────────────────────────┘
                           │
                           ▼
      Solana RPC: getAccountInfo(pair_address)
                           │
                           ▼
┌──────────────────────────────────────────────────────────────┐
│  Крок 3: Витягнути owner з відповіді                         │
└──────────────────────────────────────────────────────────────┘
                           │
                           ▼
      result.value.owner
      └─> "pAMMBay6oceH9fJKBRHGP5D4bD4sWpmSwMn52FMfXEA"
```

### Приклад Solana RPC відповіді:

```json
{
  "jsonrpc": "2.0",
  "result": {
    "context": {
      "slot": 371598587
    },
    "value": {
      "data": {...},
      "executable": false,
      "lamports": 2039280,
      "owner": "pAMMBay6oceH9fJKBRHGP5D4bD4sWpmSwMn52FMfXEA",  // ✅ Знайдено тут
      "rentEpoch": 0,
      "space": 165
    }
  }
}
```

### Результат:

```json
{
  "lp_owner": "pAMMBay6oceH9fJKBRHGP5D4bD4sWpmSwMn52FMfXEA"
}
```

**Звідки береться:**
- 📊 **Джерело 1:** DexScreener API (`pairs[0].pairAddress`)
- 📊 **Джерело 2:** Solana RPC (`getAccountInfo`)
- 🔑 **Поле:** `result.value.owner`
- 📝 **Що це:** Адреса власника Liquidity Pool (зазвичай програма AMM)

**Типові LP owners:**
- `pAMMBay...` - Pump.fun AMM
- `675kPX9MHTjS2zt1qfr1NYHuzeLXfQM9H24wFSUt1Mp8` - Raydium V4
- `CAMMCzo5YL8w4VFF8KVHrK22GGUsp5VTaW7grrKgrWqK` - Orca

---

## 📊 ПОВНИЙ ПОТІК SECURITY АНАЛІЗУ

```
┌─────────────────────────────────────────────────────────────┐
│              run_analysis_cycle() - рядок 896                │
└─────────────────────────────────────────────────────────────┘
                           │
           ┌───────────────┴───────────────┐
           │                               │
           ▼                               ▼
    ┌──────────────┐            ┌──────────────────┐
    │  Jupiter API │            │  DexScreener API │
    └──────────────┘            └──────────────────┘
           │                               │
           ▼                               ▼
    ┌──────────────┐            ┌──────────────────┐
    │ jupiter_data │            │dexscreener_data  │
    └──────────────┘            └──────────────────┘
           │                               │
           └───────────────┬───────────────┘
                           │
                           ▼
        ┌──────────────────────────────────────┐
        │      Solana RPC API                  │
        │  • getTokenSupply                    │
        │  • getAccountInfo                    │
        │  • getSignaturesForAddress           │
        │  • getTransaction (parsed)           │
        └──────────────────────────────────────┘
                           │
                           ▼
                 ┌──────────────────┐
                 │ solana_rpc_data  │
                 └──────────────────┘
                           │
        ┌──────────────────┴──────────────────┐
        │                                      │
        ▼                                      ▼
┌──────────────────┐              ┌──────────────────────┐
│ _honeypot_with_  │              │ _extract_dev_from_   │
│ _fallback()      │              │ _jupiter()           │
│                  │              │                      │
│ • Jupiter Quote  │              │ jupiter_data["dev"]  │
│ • DexScreener    │              └──────────────────────┘
│ • Solana RPC     │                         │
└──────────────────┘                         │
        │                                    │
        │              ┌─────────────────────┘
        │              │
        │              │    ┌──────────────────────────┐
        │              │    │ _extract_pair_from_      │
        │              │    │ _dexscreener()           │
        │              │    │                          │
        │              │    │ pairs[0]["pairAddress"]  │
        │              │    └──────────────────────────┘
        │              │                │
        │              │                ▼
        │              │    ┌──────────────────────────┐
        │              │    │ _get_lp_owner()          │
        │              │    │                          │
        │              │    │ Solana RPC getAccountInfo│
        │              │    └──────────────────────────┘
        │              │                │
        └──────────────┴────────────────┘
                       │
                       ▼
        ┌──────────────────────────────┐
        │      security object          │
        │                               │
        │  • honeypot_check             │
        │  • lp_owner                   │
        │  • dev_address                │
        └──────────────────────────────┘
                       │
                       ▼
        ┌──────────────────────────────┐
        │   save_analysis()             │
        │   (зберігається в БД)         │
        └──────────────────────────────┘
```

---

## 🎯 ПІДСУМОК

### Honeypot Check:
- **3 методи перевірки** з fallback
- **Метод 1:** Jupiter Quote API (найточніший)
- **Метод 2:** DexScreener txns (підрахунок продажів)
- **Метод 3:** Solana RPC txs (аналіз балансів)
- **Результат:** `true` = honeypot, `false` = безпечний

### Dev Address:
- **Джерело:** Jupiter API
- **Поле:** `dev`, `dev_address`, або `devAddress`
- **Результат:** Solana адреса розробника

### LP Owner:
- **Джерело 1:** DexScreener (pair address)
- **Джерело 2:** Solana RPC (getAccountInfo)
- **Результат:** Адреса власника LP (програма AMM)

---

## 📝 ВАЖЛИВІ МОМЕНТИ

1. **Honeypot check використовує fallback:**
   - Якщо Jupiter не працює → DexScreener
   - Якщо DexScreener не працює → Solana RPC
   - Результати комбінуються

2. **DexScreener має пріоритет над RPC:**
   - Якщо DexScreener каже "sells > 0" → NOT honeypot
   - Навіть якщо RPC не знайшов продажів

3. **LP Owner потребує 2 запити:**
   - Спочатку pair_address з DexScreener
   - Потім owner з Solana RPC

4. **Dev Address найпростіший:**
   - Просто витягується з Jupiter відповіді
   - Не потребує додаткових запитів

