# 📋 Умови Автопокупки та Ручної Покупки

Детальний опис всіх перевірок, які виконуються перед покупкою токена.

---

## 🤖 АВТОПОКУПКА (Auto-Buy)

### 📍 Де Виконується:
- **Файл:** `server/_v3_analyzer_jupiter.py`
- **Метод:** `save_token_data()` (рядки 1000-1102)
- **Цикл:** Кожну секунду через `_scan_loop()`

### ✅ УМОВИ ДЛЯ АВТОПОКУПКИ (ВСІ ОБОВ'ЯЗКОВІ):

#### 1. **Є увімкнені кошельки**
```sql
SELECT COUNT(*) FROM wallets 
WHERE entry_amount_usd IS NOT NULL AND entry_amount_usd > 0
```
- **Умова:** `enabled_wallet_count > 0`
- **Рядок коду:** 1008-1011
- **Якщо не виконано:** Автопокупка не спрацює взагалі

#### 2. **Вік токена (ітерації)**
```sql
SELECT COUNT(*) FROM token_metrics_seconds
WHERE token_id=$1 AND usd_price IS NOT NULL AND usd_price > 0
```
- **Умова:** `iterations >= AUTO_BUY_ENTRY_SEC`
- **Константа:** `AUTO_BUY_ENTRY_SEC = 171` секунд (config.py:131)
- **Рядок коду:** 1044, 1050
- **Якщо не виконано:** Токен ще занадто молодий, чекаємо

#### 3. **Немає відкритої позиції**
```sql
SELECT NOT EXISTS (
  SELECT 1 FROM wallet_history 
  WHERE token_id=$1 AND exit_iteration IS NULL
) AS none
```
- **Умова:** `no_entry.none = TRUE`
- **Рядок коду:** 1019-1022, 1043
- **Якщо не виконано:** Вже є відкрита позиція, не можна купити вдруге

#### 4. **Токен не в архіві**
- **Умова:** `history_ready = FALSE`
- **Рядок коду:** 1031
- **Якщо не виконано:** Токен вже архівований, не обробляємо

#### 5. **AI Decision = "buy"**
- **Умова:** `pattern_segment_decision = "buy"`
- **Рядок коду:** 1056, 1078
- **Якщо не виконано:** AI вирішив "not", не купуємо

#### 6. **Pattern Segments дозволяють вхід**
- **Умова:** `_segments_allow_entry(segments) = TRUE`
- **Рядок коду:** 1079
- **Логіка `_segments_allow_entry()` (рядки 219-254):**
  - ❌ Якщо є "unknown", "bad", "risk", "flat" → `False`
  - ❌ Якщо 2+ сегменти = "middle" → `False`
  - ✅ Якщо всі сегменти = "best"/"good"/"super" → `True`
  - ✅ Якщо 1 "middle" в першому/другому сегменті + останній = "good"/"best" → `True`

#### 7. **Мінімальна кількість транзакцій**
- **Умова:** `total_tx >= MIN_TX_COUNT`
- **Константа:** `MIN_TX_COUNT = 100` транзакцій (config.py:205)
- **Розрахунок:** `total_tx = num_buys_24h + num_sells_24h`
- **Рядок коду:** 1057, 1063, 1080
- **Якщо не виконано:** Замало активності, можливий мертвий токен

#### 8. **Мінімальна частка продажів (Anti-Honeypot)**
- **Умова:** `sell_share >= MIN_SELL_SHARE`
- **Константа:** `MIN_SELL_SHARE = 0.20` (20%) (config.py:206)
- **Розрахунок:** `sell_share = num_sells_24h / total_tx`
- **Рядок коду:** 1058-1062, 1064, 1081
- **Якщо не виконано:** Замало продажів → можливий honeypot (тільки покупки)

#### 9. **Ціна > 0**
```sql
SELECT usd_price FROM token_metrics_seconds
WHERE token_id=$1 AND usd_price IS NOT NULL
ORDER BY ts DESC LIMIT 1
```
- **Умова:** `latest_price > 0`
- **Рядок коду:** 1065-1076, 1082
- **Якщо не виконано:** Токен мертвий (ціна = 0)

### 🔄 Процес Автопокупки:

```python
# 1. Перевірка всіх умов (рядки 1008-1083)
if (enabled_wallet_count > 0
    and iterations >= AUTO_BUY_ENTRY_SEC
    and no_entry = TRUE
    and history_ready = FALSE
    and decision == "buy"
    and _segments_allow_entry(segments) == TRUE
    and total_tx >= MIN_TX_COUNT
    and sell_share >= MIN_SELL_SHARE
    and latest_price > 0):
    
    # 2. Виклик buy_real() в background task (неблокуючий)
    asyncio.create_task(_auto_buy_task())
    # → buy_real(token_id, source='auto_buy')
```

---

## 🖐️ РУЧНА ПОКУПКА (Force Buy)

### 📍 Де Виконується:
- **HTTP Endpoint:** `POST /api/buy/force?token_id=123` (main.py:471-488)
- **Функція:** `force_buy()` → `buy_real(source='force_buy')` (_v2_buy_sell.py:1019-1038)

### ✅ УМОВИ ДЛЯ РУЧНОЇ ПОКУПКИ:

#### ❌ **BYPASS ВСІХ ПЕРЕВІРОК АНАЛІЗАТОРА:**
- ❌ Не перевіряє `AUTO_BUY_ENTRY_SEC` (можна купити навіть 1 секунду)
- ❌ Не перевіряє `pattern_segment_decision` (можна купити навіть "not")
- ❌ Не перевіряє `_segments_allow_entry()` (можна купити навіть "bad" segments)
- ❌ Не перевіряє `MIN_TX_COUNT` (можна купити навіть 0 транзакцій)
- ❌ Не перевіряє `MIN_SELL_SHARE` (можна купити навіть 0% продажів)
- ❌ Не перевіряє `latest_price > 0` (можна купити навіть ціну = 0)

#### ✅ **ПЕРЕВІРКИ В `buy_real()` (ЗАВЖДИ ВИКОНУЮТЬСЯ):**

##### 1. **Токен існує**
```sql
SELECT token_address, decimals, wallet_id FROM tokens WHERE id=$1
```
- **Умова:** Токен знайдено в БД
- **Рядок коду:** 882-888
- **Якщо не виконано:** `{"success": False, "message": "Token not found"}`

##### 2. **Немає відкритої позиції**
```sql
SELECT id FROM wallet_history
WHERE token_id=$1 AND exit_iteration IS NULL LIMIT 1
```
- **Умова:** Немає відкритої позиції
- **Рядок коду:** 892-902
- **Якщо не виконано:** `{"success": False, "message": "Position already open - cannot enter again"}`

##### 3. **Є вільний кошелек**
- **Умова:** `get_free_wallet()` повертає кошелек
- **Перевірки в `get_free_wallet()` (рядки 84-215):**
  - ✅ Кошелек не використовується іншим токеном (`wallet_id IS NULL`)
  - ✅ Немає відкритої позиції в `wallet_history`
  - ✅ `entry_amount_usd > 0` (кошелек увімкнено)
- **Рядок коду:** 905-908
- **Якщо не виконано:** `{"success": False, "message": "No free real wallet available"}`

##### 4. **Honeypot Check (ЗАВЖДИ ВИКОНУЄТЬСЯ!)**
- **Де:** `execute_buy()` (рядки 220-421)
- **Логіка:**
  1. Симуляція продажу 1000 токенів (рядок 253)
  2. Отримання quote для продажу (рядки 259-275)
  3. Побудова swap транзакції з dummy pubkey (рядки 278-293)
  4. Симуляція транзакції через RPC (рядки 295-320)
  5. Перевірка результату симуляції (рядки 321-330)
- **Умова:** Симуляція продажу успішна
- **Рядок коду:** 251-330
- **Якщо не виконано:** `{"success": False, "message": "Honeypot detected: ..."}`

##### 5. **Баланс SOL достатній**
- **Перевірки:**
  - `balance_sol > 0` (рядок 243-244)
  - `sol_need <= balance_sol * 0.95` (залишаємо 5% для комісій) (рядок 248-249)
  - Додаткова перевірка з урахуванням ATA rent (~0.002 SOL) (рядки 231-246)
- **Рядок коду:** 241-249
- **Якщо не виконано:** `{"success": False, "message": "Insufficient SOL balance ..."}`

##### 6. **Реальна покупка через Jupiter**
- Отримання quote для покупки (рядки 332-350)
- Побудова swap транзакції (рядки 352-365)
- Підпис транзакції з keypair (рядки 367-375)
- Відправка транзакції до блокчейну (рядки 377-410)
- Отримання signature (рядок 401, 408)

##### 7. **Перевірка signature**
- **Умова:** `signature` існує (не `None`)
- **Рядок коду:** 939-944
- **Якщо не виконано:** `{"success": False, "message": "Buy transaction returned success but no signature"}`

##### 8. **Запис в БД**
- Оновлення `tokens.wallet_id` (рядки 961-973)
- Запис в `wallet_history` (рядки 975-1003)

---

## 📊 Порівняння Автопокупки vs Ручна Покупка

| Перевірка | Автопокупка | Ручна Покупка |
|-----------|-------------|---------------|
| **AUTO_BUY_ENTRY_SEC** | ✅ Перевіряє (171s) | ❌ Bypass |
| **Немає відкритої позиції** | ✅ Перевіряє | ✅ Перевіряє |
| **AI Decision = "buy"** | ✅ Перевіряє | ❌ Bypass |
| **_segments_allow_entry()** | ✅ Перевіряє | ❌ Bypass |
| **MIN_TX_COUNT** | ✅ Перевіряє (100) | ❌ Bypass |
| **MIN_SELL_SHARE** | ✅ Перевіряє (20%) | ❌ Bypass |
| **latest_price > 0** | ✅ Перевіряє | ❌ Bypass |
| **Токен існує** | ✅ Перевіряє | ✅ Перевіряє |
| **Є вільний кошелек** | ✅ Перевіряє | ✅ Перевіряє |
| **Honeypot Check** | ✅ Перевіряє | ✅ Перевіряє |
| **Баланс SOL** | ✅ Перевіряє | ✅ Перевіряє |
| **Signature exists** | ✅ Перевіряє | ✅ Перевіряє |

---

## 🔒 Honeypot Check (Завжди Виконується)

**Важливо:** Honeypot check виконується **ЗАВЖДИ**, навіть для ручної покупки!

### Алгоритм:
1. Симуляція продажу 1000 токенів
2. Отримання quote для продажу через Jupiter API
3. Побудова swap транзакції з dummy pubkey (`1111...1111`)
4. Симуляція транзакції через Solana RPC
5. Перевірка результату:
   - ✅ Якщо симуляція успішна → honeypot check пройшов → продовжуємо з покупкою
   - ❌ Якщо симуляція не вдалася → honeypot detected → блокування покупки

### Retry Logic:
- **MAX_RETRIES = 3** (рядок 232)
- **RETRY_DELAY = (1, 3)** секунд (рядок 233)
- Якщо перша спроба не вдалася → чекаємо 1-3 секунди → повторюємо

---

## 💰 Entry Amount (Сума Покупки)

### Автопокупка та Ручна Покупка:
1. **Спочатку:** Читає `entry_amount_usd` з таблиці `wallets` для обраного кошелька
2. **Якщо не встановлено:** Використовує `DEFAULT_ENTRY_AMOUNT_USD = 5.0` (config.py:167)
3. **Рядок коду:** 916-927

### Важливо:
- Кожен кошелек має свою `entry_amount_usd` (налаштовується через UI)
- Якщо `entry_amount_usd = 0` → кошелек вимкнено з автопокупки
- Для ручної покупки також використовується `entry_amount_usd` з кошелька

---

## 📝 Константи (config.py)

```python
AUTO_BUY_ENTRY_SEC = 171        # Секунд до автопокупки
MIN_TX_COUNT = 100               # Мінімальна кількість транзакцій
MIN_SELL_SHARE = 0.20            # Мінімальна частка продажів (20%)
DEFAULT_ENTRY_AMOUNT_USD = 5.0   # Сума покупки за замовчуванням
```

---

## ⚠️ Важливі Примітки

1. **Автопокупка виконується в background task** (неблокуючий) → не гальмує аналізатор
2. **Honeypot check завжди виконується** → навіть для ручної покупки
3. **Ручна покупка bypass всіх перевірок аналізатора** → можна купити будь-який токен
4. **Перевірка signature критична** → якщо signature відсутній, покупка не записується в БД
5. **Баланс перевіряється з урахуванням ATA rent** → ~0.002 SOL для створення Associated Token Account

